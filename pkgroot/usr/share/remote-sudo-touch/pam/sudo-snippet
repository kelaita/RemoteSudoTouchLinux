# RemoteSudoTouch sudo approval
#
# Manual fallback snippet. Prefer `sudo remote-sudo-touch-setup`, which verifies
# the bridge path first and inserts a managed block automatically.
#
# It returns success only when the Mac-side Touch ID approval succeeds.
auth sufficient pam_exec.so quiet seteuid /usr/lib/remote-sudo-touch/remote-sudo-touch
