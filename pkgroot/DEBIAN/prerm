#!/bin/bash
set -euo pipefail

cat <<'MSG'
remote-sudo-touch is being removed.
Run 'sudo remote-sudo-touch-disable' first if you want to remove
the managed PAM block and stop the local socket bridge cleanly.
MSG
